# GRASP: Зацепление и Связность в JavaScript и Node.js

## Обзор

В этой лекции рассматриваются два ключевых принципа GRASP (General Responsibility Assignment Software Patterns) - принципы распределения ответственности:

1. **Низкое зацепление (Low Coupling)** - минимизация зависимостей между модулями
2. **Высокая связность (High Cohesion)** - максимизация связности внутри модулей

Эти принципы помогают создавать гибкую, понятную и легко изменяемую архитектуру приложений на Node.js и JavaScript.

---

## Основные концепции

### Что такое Зацепление (Coupling)?

**Зацепление (Coupling)** - это степень взаимозависимости между различными модулями, классами или компонентами системы.

**Цель**: Минимизировать зацепление - создать **мало и желательно ослабленных связей между модулями**.

**Признаки сильного зацепления:**
- Модуль знает внутреннюю структуру другого модуля
- Модуль знает сигнатуры методов других модулей
- Модуль знает структуру базы данных или внешних API
- Изменение в одном модуле требует изменений в других модулях

### Что такое Связность (Cohesion)?

**Связность (Cohesion)** - это степень, с которой элементы внутри одного модуля работают вместе для выполнения единой задачи.

**Цель**: Максимизировать связность - создать **много сильных связей внутри модуля**.

**Признаки высокой связности:**
- Все компоненты модуля работают для достижения одной цели
- Методы класса используют общие данные и тесно взаимодействуют
- Функции модуля решают связанные задачи
- Код внутри модуля легко понять как единое целое

### Визуальное представление

```
┌─────────────────────┐        ┌─────────────────────┐
│     Модуль A        │        │     Модуль B        │
│  ╔═══════════╗      │ связь  │  ╔═══════════╗      │
│  ║  высокая  ║      │◄──────►│  ║  высокая  ║      │
│  ║ связность ║      │ мини-  │  ║ связность ║      │
│  ║  внутри   ║      │ мальна │  ║  внутри   ║      │
│  ╚═══════════╝      │        │  ╚═══════════╝      │
└─────────────────────┘        └─────────────────────┘
    ХОРОШО: Высокая связность внутри, низкое зацепление между
```

---

## Пример 1: Проблема сильного зацепления (класс Person)

### Плохой пример: Класс с высоким зацеплением

```javascript
// person.js - ПЛОХОЙ ПРИМЕР с высоким зацеплением
class Person {
  constructor() {
    this.name = '';
    this.surname = '';
    this.address = '';
  }

  // Фабричный метод (Factory Method)
  static async load(id) {
    // ❌ ПРОБЛЕМА 1: Зацепление на базу данных
    // Класс Person знает о существовании модуля db
    // Класс знает контракт метода db.query()
    const data = await db.query(
      'SELECT * FROM Person WHERE id = $1', // ❌ ПРОБЛЕМА 2: Знание структуры БД
      [id]
    );

    // ❌ ПРОБЛЕМА 3: Знание структуры объекта из БД
    // Мы предполагаем, что data имеет поля name, surname, address
    const person = new Person();
    person.name = data.name;
    person.surname = data.surname;
    person.address = data.address;

    return person;
  }
}
```

### Анализ проблем зацепления

```javascript
// ЦВЕТОВОЕ КОДИРОВАНИЕ зацеплений:
// 🔵 СИНИЙ - элементы класса Person (внутренняя связность)
// 🔴 КРАСНЫЙ - внешние зависимости (зацепление)

class Person {  // 🔵 Собственный класс
  constructor() {  // 🔵 Собственный конструктор
    this.name = '';  // 🔵 Собственное поле
    this.surname = '';  // 🔵 Собственное поле
    this.address = '';  // 🔵 Собственное поле
  }

  static async load(id) {
    // 🔴 db - внешний модуль (зацепление)
    // 🔴 query - знание метода внешнего модуля (зацепление)
    // 🔴 Сигнатура: (sql, params) => Promise (зацепление на контракт)
    const data = await db.query(
      'SELECT * FROM Person WHERE id = $1',  // 🔴 Знание SQL и структуры БД
      [id]
    );

    const person = new Person();  // 🔵 Работа с собственным классом

    // 🔴 Знание полей объекта из БД (name, surname, address)
    person.name = data.name;
    person.surname = data.surname;
    person.address = data.address;

    return person;
  }
}
```

### Последствия сильного зацепления

**Проблема 1**: Невозможность тестирования без БД
```javascript
// ❌ Для теста нужна настоящая БД или сложные моки
test('Person.load should create person', async () => {
  // Как протестировать без реальной БД?
  const person = await Person.load(1);
  expect(person.name).toBe('John');
});
```

**Проблема 2**: Невозможность смены БД без изменения класса
```javascript
// ❌ Если меняем PostgreSQL на MongoDB - нужно менять Person
// ❌ Если меняется структура таблицы - нужно менять Person
// ❌ Если меняется API доступа к данным - нужно менять Person
```

**Проблема 3**: Класс Person знает слишком много
```javascript
// Класс Person зацеплен на:
// 1. Модуль db
// 2. Метод db.query() и его сигнатуру
// 3. Контракт Promise (async/await)
// 4. Структуру таблицы Person в БД
// 5. Названия полей в БД
```

---

## Пример 2: Middleware с высоким зацеплением (Express)

### Плохой пример: Зацепленные middleware

#### Файл: logger.js

```javascript
// logger.js - middleware для логирования
const writeLog = (method, url, referrer, time) => {
  // Упрощенная реализация логирования
  console.log(`${method} ${url} ${referrer} ${time}ms`);
  // В реальности здесь была бы запись в файлы, БД, ротация логов и т.д.
};

const logger = (req, res, next) => {
  // ✅ Засекаем время начала обработки запроса
  const start = Date.now();

  // ❌ ПРОБЛЕМА 1: Примешивание свойства к объекту request
  // Создаем зависимость - другие middleware будут ожидать это поле
  req.requestTime = start;

  // ❌ ПРОБЛЕМА 2: Подписка на событие объекта response
  // Логгер "лезет" во внутренности HTTP протокола
  res.on('finish', () => {
    const elapsed = Date.now() - start;

    // ❌ ПРОБЛЕМА 3: Зацепление на структуру headers
    // Если headers.referer не существует - получим undefined
    writeLog(
      req.method,  // Зацепление на req.method
      req.url,     // Зацепление на req.url
      req.headers.referer,  // Зацепление на req.headers.referer
      elapsed
    );
  });

  next();  // Передаем управление следующему middleware
};

module.exports = { logger };
```

#### Файл: timeout.js

```javascript
// timeout.js - middleware для обработки таймаута
const timeout = (ms) => {
  // Возвращаем middleware с замыканием на ms
  return (req, res, next) => {
    // ❌ ПРОБЛЕМА 1: Зависимость от logger middleware
    // timeout ожидает, что req.requestTime уже установлен
    // Без logger этот код сломается!
    setTimeout(() => {
      const elapsed = Date.now() - req.requestTime;  // ❌ req.requestTime может быть undefined!

      // ❌ ПРОБЛЕМА 2: Зацепление на response.headersSent
      // Нужно знать внутренние детали HTTP протокола
      if (!res.headersSent) {
        // Если заголовки еще не отправлены, отправляем таймаут
        res.status(503).send(`Timeout reached: ${elapsed}ms`);
      }
      // ❌ ПРОБЛЕМА 3: Таймаут никогда не очищается
      // Даже если запрос обработан успешно, setTimeout все равно выполнится
    }, ms);

    next();
  };
};

module.exports = { timeout };
```

#### Файл: main.js

```javascript
// main.js - главный файл приложения
const express = require('express');
const { logger } = require('./logger.js');
const { timeout } = require('./timeout.js');

const app = express();

// ❌ ВАЖНО: Порядок middleware критичен!
// logger ДОЛЖЕН быть перед timeout, иначе req.requestTime будет undefined
app.use(logger);      // Должен быть первым!
app.use(timeout(1000)); // Зависит от logger!

// Роуты
app.get('/', (req, res) => {
  res.send('Hello World');
});

app.get('/page1', (req, res) => {
  // Эта страница будет отдаваться 5 секунд
  // Таймаут сработает через 1 секунду
  setTimeout(() => {
    res.send('Hello World from page1');
  }, 5000);
});

app.listen(8000, () => {
  console.log('Server listening on port 8000');
});
```

### Анализ проблем зацепления в middleware

#### Граф зависимостей (плохой пример)

```
┌──────────────┐
│   logger     │
│  middleware  │
└──────┬───────┘
       │ примешивает
       │ req.requestTime
       ▼
┌──────────────┐
│   request    │ ◄────────┐
│   object     │          │ читает
└──────────────┘          │ req.requestTime
                          │
                    ┌─────┴──────┐
                    │  timeout   │
                    │ middleware │
                    └────────────┘
                          │
                          │ читает
                          │ res.headersSent
                          ▼
                    ┌──────────────┐
                    │  response    │
                    │   object     │
                    └──────────────┘

❌ ПРОБЛЕМЫ:
1. timeout зависит от logger (через req.requestTime)
2. Оба зависят от request/response (через поля и события)
3. Порядок middleware критичен
4. Невозможно использовать timeout без logger
```

### Последствия сильного зацепления в middleware

**Проблема 1**: Хрупкость при изменении порядка
```javascript
// ❌ Если закомментировать logger - timeout сломается
// app.use(logger);  // Закомментировали
app.use(timeout(1000));

// Результат:
// TypeError: Cannot read property 'requestTime' of undefined
// потому что req.requestTime не установлен
```

**Проблема 2**: Невозможность повторного использования
```javascript
// ❌ Нельзя использовать timeout в другом проекте без logger
// ❌ Нельзя заменить logger на другую реализацию без проверки timeout
```

**Проблема 3**: Скрытые зависимости
```javascript
// ❌ Неочевидно из сигнатуры, что timeout зависит от logger
// Разработчик может не знать, что нужно подключать logger первым
```

---

## Решение: Снижение зацепления

### Хороший пример: Независимые модули

#### Файл: delay.js

```javascript
// delay.js - утилита для создания задержки
// ✅ Не зависит ни от каких внешних модулей
// ✅ Простой, понятный контракт
// ✅ Легко тестируется

const delay = (ms) => {
  return new Promise((resolve) => {
    setTimeout(resolve, ms);
  });
};

module.exports = { delay };
```

**Преимущества:**
- Нет зависимостей от HTTP, request, response
- Универсальная утилита - можно использовать где угодно
- Простой контракт: `(ms: number) => Promise<void>`

#### Файл: timeout.js (улучшенная версия)

```javascript
// timeout.js - функция для обработки таймаута
// ✅ Не знает о request/response
// ✅ Не знает о HTTP протоколе
// ✅ Работает только со временем

const { delay } = require('./delay.js');

const timeout = async (ms) => {
  // Засекаем начальное время
  const start = Date.now();

  // Ждем указанное количество миллисекунд
  await delay(ms);

  // Вычисляем реальное время ожидания
  const elapsed = Date.now() - start;

  // Возвращаем сообщение о таймауте
  return `Timeout reached: ${elapsed}ms`;
};

module.exports = { timeout };
```

**Преимущества:**
- ✅ Зависит только от `delay` (нижестоящий уровень абстракции)
- ✅ Не зацеплен на HTTP, request, response
- ✅ Не использует примеси (mixins)
- ✅ Простой контракт: `(ms: number) => Promise<string>`
- ✅ Легко тестируется

#### Файл: logger.js (улучшенная версия)

```javascript
// logger.js - функция для логирования
// ✅ Не знает о request/response
// ✅ Не знает о HTTP протоколе
// ✅ Принимает только необходимые данные

const logger = (method, url, referrer, time) => {
  // Формируем сообщение лога
  const message = `${method} ${url} ${referrer || 'undefined'} ${time}ms`;

  // В реальности здесь была бы более сложная логика:
  // - запись в файлы
  // - отправка в систему мониторинга
  // - ротация логов
  // - форматирование
  console.log(message);
};

module.exports = { logger };
```

**Преимущества:**
- ✅ Не зацеплен на request/response
- ✅ Принимает только необходимые параметры
- ✅ Простой контракт: `(method, url, referrer, time) => void`
- ✅ Легко тестируется с любыми входными данными

#### Файл: main.js (улучшенная версия)

```javascript
// main.js - главный контроллер приложения
// Здесь происходит СБОРКА компонентов
// Здесь высокая СВЯЗНОСТЬ, но низкое ЗАЦЕПЛЕНИЕ модулей

const http = require('http');
const { logger } = require('./logger.js');
const { timeout } = require('./timeout.js');
const { delay } = require('./delay.js');

// Роутинг - маппинг URL на обработчики
const routing = {
  '/': async () => 'Hello World',
  '/page1': async () => {
    await delay(5000);
    return 'Hello World from page1';
  },
  '/other': async () => 'Other page',
};

// ✅ Универсальный контроллер
// Здесь происходит интеграция всех компонентов
const handler = async (req, res) => {
  // 1️⃣ Засекаем время (локальная переменная!)
  const start = Date.now();

  // 2️⃣ Подписываемся на событие закрытия соединения
  res.on('finish', () => {
    const elapsed = Date.now() - start;

    // ✅ Вызываем logger с явными параметрами
    // Логгер не зависит от request/response!
    logger(
      req.method,
      req.url,
      req.headers.referer,
      elapsed
    );
  });

  // 3️⃣ Находим обработчик для URL
  const route = routing[req.url];

  if (!route) {
    res.statusCode = 404;
    res.end('Not found');
    return;
  }

  // 4️⃣ Гонка между обработчиком и таймаутом
  // ✅ Используем Promise.race для элегантной обработки
  try {
    const result = await Promise.race([
      route(),           // Вызываем обработчик маршрута
      timeout(1000)      // Ждем таймаут 1 секунда
    ]);

    // 5️⃣ Проверяем, не отправлены ли уже заголовки
    // (для предотвращения гонки условий)
    if (!res.headersSent) {
      res.end(result);
    }
  } catch (error) {
    if (!res.headersSent) {
      res.statusCode = 500;
      res.end('Internal Server Error');
    }
  }
};

// Создаем сервер
const server = http.createServer(handler);

// Запускаем сервер
server.listen(8000, () => {
  console.log('Server started on port 8000');
});

module.exports = { server };
```

### Анализ улучшений

#### Граф зависимостей (хороший пример)

```
                    ┌──────────────┐
                    │    delay     │
                    │   (базовая   │
                    │  утилита)    │
                    └──────┬───────┘
                           │
                           │ использует
                           ▼
                    ┌──────────────┐
                    │   timeout    │
                    │  (утилита)   │
                    └──────────────┘


┌──────────────┐
│    logger    │
│  (утилита)   │
└──────────────┘


        ▲                    ▲
        │                    │
        │   вызывает        │ вызывает
        │                    │
        └────────┬───────────┘
                 │
           ┌─────┴──────┐
           │  handler   │ ◄──── request/response
           │ (контроллер│       знание только здесь
           │   сборки)  │
           └────────────┘

✅ ПРЕИМУЩЕСТВА:
1. timeout НЕ зависит от logger
2. logger НЕ зависит от request/response
3. timeout НЕ зависит от HTTP
4. Порядок не важен - зависимости явные
5. Каждый модуль можно использовать отдельно
```

### Сравнение: До и После

#### До: Middleware подход (высокое зацепление)

```javascript
// ❌ ПЛОХО: Зацепление через примеси и события

// logger.js
const logger = (req, res, next) => {
  req.requestTime = Date.now();  // ❌ Примесь
  res.on('finish', () => {       // ❌ Знание о событиях
    // ...логика
  });
  next();
};

// timeout.js
const timeout = (ms) => (req, res, next) => {
  setTimeout(() => {
    const elapsed = Date.now() - req.requestTime;  // ❌ Зависимость от logger
    if (!res.headersSent) {                        // ❌ Знание о HTTP
      res.status(503).send(`Timeout: ${elapsed}ms`);
    }
  }, ms);
  next();
};

// main.js
app.use(logger);        // ❌ Порядок критичен!
app.use(timeout(1000)); // ❌ Должен быть после logger!
```

**Проблемы:**
- ❌ timeout зависит от logger через req.requestTime
- ❌ Невозможно использовать timeout без logger
- ❌ Порядок middleware критичен
- ❌ Оба знают о деталях HTTP протокола
- ❌ Сложно тестировать отдельно

#### После: Функциональный подход (низкое зацепление)

```javascript
// ✅ ХОРОШО: Независимые модули с явными зависимостями

// logger.js
const logger = (method, url, referrer, time) => {
  console.log(`${method} ${url} ${referrer} ${time}ms`);
};

// timeout.js
const { delay } = require('./delay.js');
const timeout = async (ms) => {
  const start = Date.now();
  await delay(ms);
  return `Timeout: ${Date.now() - start}ms`;
};

// main.js - ТОЛЬКО здесь знание о HTTP
const handler = async (req, res) => {
  const start = Date.now();

  res.on('finish', () => {
    logger(req.method, req.url, req.headers.referer, Date.now() - start);
  });

  const result = await Promise.race([
    routing[req.url](),
    timeout(1000)
  ]);

  res.end(result);
};
```

**Преимущества:**
- ✅ logger не зависит от HTTP
- ✅ timeout не зависит от logger
- ✅ Порядок не важен
- ✅ Каждый модуль можно использовать отдельно
- ✅ Легко тестировать
- ✅ Знание о HTTP только в контроллере

---

## Ключевые принципы снижения зацепления

### 1. Избегайте примесей (Mixins)

**Плохо:**
```javascript
// ❌ Примешивание свойств к чужим объектам
const middleware = (req, res, next) => {
  req.customProperty = 'value';  // ❌ Создает скрытую зависимость
  res.customMethod = () => {};   // ❌ Другие компоненты могут зависеть от этого
  next();
};
```

**Хорошо:**
```javascript
// ✅ Используйте локальные переменные или замыкания
const handler = (req, res) => {
  const context = {              // ✅ Локальный контекст
    customProperty: 'value',
    customMethod: () => {}
  };

  // Работаем с локальными данными
  processRequest(context, req, res);
};
```

### 2. Используйте явные зависимости

**Плохо:**
```javascript
// ❌ Неявная зависимость через глобальное состояние
const timeout = (ms) => (req, res, next) => {
  // ❌ Откуда взялся req.requestTime? Неочевидно!
  const elapsed = Date.now() - req.requestTime;
  next();
};
```

**Хорошо:**
```javascript
// ✅ Явные параметры
const timeout = async (ms, startTime) => {
  await delay(ms);
  const elapsed = Date.now() - startTime;
  return `Timeout: ${elapsed}ms`;
};

// ✅ Вызов с явными параметрами
const handler = async (req, res) => {
  const start = Date.now();
  const result = await timeout(1000, start);  // ✅ Понятно, откуда берутся данные
};
```

### 3. Разделяйте ответственности

**Плохо:**
```javascript
// ❌ Класс Person знает о БД, HTTP, валидации
class Person {
  async save() {
    await db.query('INSERT ...', [this.name]);  // ❌ Знание о БД
  }

  async loadFromRequest(req) {
    this.name = req.body.name;                   // ❌ Знание о HTTP
  }

  validate() {
    if (!this.name) throw new Error('...');     // ❌ Смешанные обязанности
  }
}
```

**Хорошо:**
```javascript
// ✅ Разделение на слои
class Person {
  constructor(name, surname, address) {
    this.name = name;
    this.surname = surname;
    this.address = address;
  }
}

// ✅ Репозиторий - работа с БД
class PersonRepository {
  async save(person) {
    await db.query('INSERT ...', [person.name]);
  }
}

// ✅ Контроллер - работа с HTTP
class PersonController {
  async create(req, res) {
    const person = new Person(req.body.name, req.body.surname, req.body.address);
    await personRepository.save(person);
    res.json(person);
  }
}

// ✅ Валидатор - проверка данных
class PersonValidator {
  validate(person) {
    if (!person.name) throw new Error('Name is required');
  }
}
```

### 4. Программируйте на интерфейсы, а не реализации

**Плохо:**
```javascript
// ❌ Зацепление на конкретную реализацию
class UserService {
  constructor() {
    this.db = new PostgreSQLDatabase();  // ❌ Жесткая зависимость
  }

  async getUser(id) {
    return this.db.query('SELECT * FROM users WHERE id = $1', [id]);
  }
}
```

**Хорошо:**
```javascript
// ✅ Зависимость от абстракции (интерфейса)
class UserService {
  constructor(database) {  // ✅ Внедрение зависимости
    this.db = database;
  }

  async getUser(id) {
    // ✅ Работаем через интерфейс, не важна конкретная реализация
    return this.db.findById('users', id);
  }
}

// Использование:
const postgresDb = new PostgreSQLDatabase();
const mongoDb = new MongoDatabase();

const service1 = new UserService(postgresDb);  // ✅ Можем использовать PostgreSQL
const service2 = new UserService(mongoDb);     // ✅ Или MongoDB - сервис не меняется
```

---

## Связность внутри модулей

### Высокая связность в контроллере

```javascript
// main.js - контроллер с высокой связностью
// ✅ Все элементы работают вместе для обработки HTTP запросов

const handler = async (req, res) => {
  // Все переменные и функции тесно связаны
  const start = Date.now();           // ⭐ Связано с логированием

  res.on('finish', () => {            // ⭐ Связано с start и logger
    const elapsed = Date.now() - start;
    logger(req.method, req.url, req.headers.referer, elapsed);
  });

  const route = routing[req.url];     // ⭐ Связано с req и result

  const result = await Promise.race([ // ⭐ Связано с route и timeout
    route(),
    timeout(1000)
  ]);

  if (!res.headersSent) {             // ⭐ Связано с result и res
    res.end(result);
  }
};
```

**Характеристики высокой связности:**
- ✅ Все переменные используются вместе для единой цели
- ✅ Логика легко понятна как единое целое
- ✅ Изменение одной части требует понимания других частей
- ✅ Все элементы необходимы для выполнения задачи

### Пример низкой связности (антипаттерн)

```javascript
// ❌ ПЛОХО: Низкая связность - несвязанные обязанности
class Utils {
  // Функции не связаны между собой

  formatDate(date) {
    return date.toISOString();
  }

  calculateTotal(items) {
    return items.reduce((sum, item) => sum + item.price, 0);
  }

  sendEmail(to, subject, body) {
    // ... отправка email
  }

  parseJSON(str) {
    return JSON.parse(str);
  }
}

// ❌ Проблема: класс Utils объединяет несвязанные функции
// Лучше разделить на отдельные модули: DateUtils, CartUtils, EmailService, JSONParser
```

---

## Слои и зацепление

### Допустимое зацепление на нижестоящие слои

```javascript
// ✅ НОРМАЛЬНО: Зацепление на нижестоящие абстракции

// Слой 1: Базовые утилиты (foundation layer)
const delay = (ms) => new Promise(resolve => setTimeout(resolve, ms));

// Слой 2: Бизнес-утилиты (utility layer)
// ✅ Зависит от delay - это НОРМАЛЬНО
const timeout = async (ms) => {
  const start = Date.now();
  await delay(ms);  // ✅ Зависимость от нижестоящего слоя
  return `Timeout: ${Date.now() - start}ms`;
};

// Слой 3: Контроллеры (application layer)
// ✅ Зависит от timeout и logger - это НОРМАЛЬНО
const handler = async (req, res) => {
  const result = await Promise.race([
    processRequest(req),
    timeout(1000)  // ✅ Зависимость от нижестоящего слоя
  ]);

  logger(...);  // ✅ Зависимость от нижестоящего слоя
  res.end(result);
};
```

**Правило:** Зацепление на нижестоящие слои абстракции - это нормально и необходимо.

### Архитектура слоев

```
┌─────────────────────────────────────┐
│   Application Layer (Контроллеры)   │
│   - HTTP handlers                   │
│   - Бизнес-логика                   │
└──────────────┬──────────────────────┘
               │ ✅ может зависеть
               ▼
┌─────────────────────────────────────┐
│   Business Layer (Сервисы)          │
│   - UserService                     │
│   - OrderService                    │
└──────────────┬──────────────────────┘
               │ ✅ может зависеть
               ▼
┌─────────────────────────────────────┐
│   Data Layer (Репозитории)          │
│   - UserRepository                  │
│   - Database access                 │
└──────────────┬──────────────────────┘
               │ ✅ может зависеть
               ▼
┌─────────────────────────────────────┐
│   Foundation Layer (Утилиты)        │
│   - delay, logger, timeout          │
│   - Встроенные модули Node.js       │
└─────────────────────────────────────┘

❌ ЗАПРЕЩЕНО: Зависимость вверх (Foundation → Application)
✅ РАЗРЕШЕНО: Зависимость вниз (Application → Foundation)
```

---

## Практические рекомендации

### 1. Контрольный список для снижения зацепления

При написании нового модуля проверьте:

- [ ] Модуль не использует глобальные переменные
- [ ] Все зависимости явно переданы через параметры или конструктор
- [ ] Модуль не примешивает свойства к чужим объектам
- [ ] Модуль не подписывается на события чужих объектов (если это не его прямая обязанность)
- [ ] Модуль не знает о структуре базы данных
- [ ] Модуль не знает о деталях HTTP протокола (если это не контроллер)
- [ ] Модуль можно протестировать изолированно
- [ ] Модуль можно использовать в другом проекте

### 2. Контрольный список для повышения связности

При рефакторинге проверьте:

- [ ] Все методы класса используют общие поля
- [ ] Все функции модуля решают одну задачу
- [ ] Название модуля отражает единую цель
- [ ] Невозможно разделить модуль на независимые части
- [ ] Все публичные методы нужны для работы модуля
- [ ] Нет методов, которые "не вписываются" в общую концепцию

### 3. Признаки проблем с зацеплением

**Вопросы для самопроверки:**

1. **Можно ли использовать модуль отдельно?**
   - ❌ Нет → высокое зацепление
   - ✅ Да → нормальное зацепление

2. **Нужны ли моки для тестирования?**
   - ❌ Нужны сложные моки для многих зависимостей → высокое зацепление
   - ✅ Моки простые или не нужны → низкое зацепление

3. **Изменение в одном модуле требует изменений в других?**
   - ❌ Да → высокое зацепление
   - ✅ Нет → низкое зацепление

4. **Есть ли скрытые зависимости?**
   - ❌ Есть (примеси, глобальные переменные) → высокое зацепление
   - ✅ Все зависимости явные → низкое зацепление

### 4. Признаки проблем со связностью

**Вопросы для самопроверки:**

1. **У класса/модуля одна ответственность?**
   - ❌ Несколько несвязанных обязанностей → низкая связность
   - ✅ Одна четкая обязанность → высокая связность

2. **Все методы используют общие данные?**
   - ❌ Некоторые методы независимы → низкая связность
   - ✅ Все методы работают с общим состоянием → высокая связность

3. **Можно ли описать модуль одним предложением?**
   - ❌ Нужно несколько предложений → низкая связность
   - ✅ Одно четкое описание → высокая связность

---

## Паттерны для управления зацеплением

### 1. Dependency Injection (Внедрение зависимостей)

```javascript
// ❌ ПЛОХО: Жесткая зависимость
class UserService {
  constructor() {
    this.logger = new FileLogger();  // ❌ Нельзя изменить
  }
}

// ✅ ХОРОШО: Внедрение зависимости
class UserService {
  constructor(logger) {  // ✅ Гибкость
    this.logger = logger;
  }
}

// Использование:
const fileLogger = new FileLogger();
const consoleLogger = new ConsoleLogger();

const service1 = new UserService(fileLogger);
const service2 = new UserService(consoleLogger);
```

### 2. Interface Segregation (Разделение интерфейсов)

```javascript
// ❌ ПЛОХО: Толстый интерфейс
class Database {
  query(sql, params) { }
  transaction(callback) { }
  backup() { }
  optimize() { }
  migrate() { }
}

// Клиент зависит от всего интерфейса
class UserRepository {
  constructor(db) {
    this.db = db;  // ❌ Знает о backup, optimize, migrate
  }

  async find(id) {
    return this.db.query('SELECT ...', [id]);  // Использует только query
  }
}

// ✅ ХОРОШО: Тонкие интерфейсы
class QueryExecutor {
  query(sql, params) { }
}

class TransactionManager {
  transaction(callback) { }
}

class DatabaseAdmin {
  backup() { }
  optimize() { }
  migrate() { }
}

// Клиент зависит только от нужного интерфейса
class UserRepository {
  constructor(queryExecutor) {  // ✅ Знает только о query
    this.db = queryExecutor;
  }

  async find(id) {
    return this.db.query('SELECT ...', [id]);
  }
}
```

### 3. Factory Pattern (Фабричный паттерн)

```javascript
// ✅ Фабрика инкапсулирует создание зависимостей
class ServiceFactory {
  static createUserService(config) {
    const logger = config.production
      ? new FileLogger()
      : new ConsoleLogger();

    const db = config.production
      ? new PostgreSQLDatabase()
      : new InMemoryDatabase();

    const repository = new UserRepository(db);
    const service = new UserService(repository, logger);

    return service;
  }
}

// Использование:
const service = ServiceFactory.createUserService({ production: true });
```

---

## Рефакторинг: Пошаговое снижение зацепления

### Шаг 1: Идентификация зацеплений

```javascript
// Исходный код с проблемами
class OrderService {
  async createOrder(req) {
    // ❌ Зацепление 1: HTTP (req)
    const userId = req.session.userId;

    // ❌ Зацепление 2: База данных
    const user = await db.query('SELECT * FROM users WHERE id = $1', [userId]);

    // ❌ Зацепление 3: Email сервис
    await sendEmail(user.email, 'Order created', '...');

    // ❌ Зацепление 4: Логгер
    console.log(`Order created for user ${userId}`);

    return { success: true };
  }
}
```

### Шаг 2: Извлечение зависимостей

```javascript
// Выделяем интерфейсы
class OrderService {
  constructor(userRepository, emailService, logger) {
    this.userRepository = userRepository;
    this.emailService = emailService;
    this.logger = logger;
  }

  async createOrder(userId) {  // ✅ Не зависит от req
    const user = await this.userRepository.findById(userId);
    await this.emailService.send(user.email, 'Order created', '...');
    this.logger.log(`Order created for user ${userId}`);

    return { success: true };
  }
}
```

### Шаг 3: Создание тонких интерфейсов

```javascript
// Репозиторий - только работа с данными
class UserRepository {
  constructor(database) {
    this.db = database;
  }

  async findById(id) {
    return this.db.query('SELECT * FROM users WHERE id = $1', [id]);
  }
}

// Email сервис - только отправка писем
class EmailService {
  async send(to, subject, body) {
    // Логика отправки
  }
}

// Логгер - только логирование
class Logger {
  log(message) {
    console.log(message);
  }
}
```

### Шаг 4: Сборка в контроллере

```javascript
// Контроллер - точка сборки зависимостей
class OrderController {
  constructor(orderService) {
    this.orderService = orderService;
  }

  async create(req, res) {
    // ✅ Контроллер знает о HTTP
    const userId = req.session.userId;

    // ✅ Сервис не знает о HTTP
    const result = await this.orderService.createOrder(userId);

    res.json(result);
  }
}

// Инициализация (composition root)
const db = new PostgreSQLDatabase();
const userRepository = new UserRepository(db);
const emailService = new EmailService();
const logger = new Logger();

const orderService = new OrderService(userRepository, emailService, logger);
const orderController = new OrderController(orderService);
```

---

## Тестирование: Преимущества низкого зацепления

### До рефакторинга (высокое зацепление)

```javascript
// ❌ Сложно тестировать из-за зацепления
class OrderService {
  async createOrder(req) {
    const userId = req.session.userId;  // Нужен mock для req
    const user = await db.query('...');  // Нужна БД или сложный mock
    await sendEmail(...);                 // Нужен mock для email
    console.log(...);                     // Нельзя проверить логи
  }
}

// ❌ Тест требует много моков
test('createOrder', async () => {
  // Сложная подготовка моков
  const mockReq = {
    session: { userId: 1 }
  };

  global.db = {
    query: jest.fn().mockResolvedValue({ email: 'test@test.com' })
  };

  global.sendEmail = jest.fn();

  const service = new OrderService();
  await service.createOrder(mockReq);

  // Проверки
});
```

### После рефакторинга (низкое зацепление)

```javascript
// ✅ Легко тестировать благодаря DI
class OrderService {
  constructor(userRepository, emailService, logger) {
    this.userRepository = userRepository;
    this.emailService = emailService;
    this.logger = logger;
  }

  async createOrder(userId) {
    const user = await this.userRepository.findById(userId);
    await this.emailService.send(user.email, '...');
    this.logger.log(`Order created for user ${userId}`);
  }
}

// ✅ Простые моки
test('createOrder', async () => {
  // Простые моки
  const mockUserRepo = {
    findById: jest.fn().mockResolvedValue({ email: 'test@test.com' })
  };

  const mockEmailService = {
    send: jest.fn()
  };

  const mockLogger = {
    log: jest.fn()
  };

  // Создаем сервис с моками
  const service = new OrderService(mockUserRepo, mockEmailService, mockLogger);

  // Тест
  await service.createOrder(1);

  // Простые проверки
  expect(mockUserRepo.findById).toHaveBeenCalledWith(1);
  expect(mockEmailService.send).toHaveBeenCalledWith('test@test.com', expect.any(String));
  expect(mockLogger.log).toHaveBeenCalled();
});
```

---

## Выводы

### Ключевые takeaways

1. **Зацепление (Coupling) vs Связность (Cohesion)**
   - Зацепление - зависимости МЕЖДУ модулями (минимизируем)
   - Связность - связи ВНУТРИ модуля (максимизируем)

2. **Признаки высокого зацепления (плохо):**
   - Модуль знает внутреннюю структуру других модулей
   - Использование примесей (mixins) для передачи данных
   - Скрытые зависимости через глобальное состояние
   - Невозможность использовать модуль отдельно
   - Сложное тестирование из-за множества зависимостей

3. **Признаки высокой связности (хорошо):**
   - Все элементы модуля работают для одной цели
   - Методы используют общее состояние
   - Модуль можно описать одним предложением
   - Невозможно разделить модуль без потери функциональности

4. **Техники снижения зацепления:**
   - Dependency Injection - внедрение зависимостей
   - Программирование на интерфейсы
   - Разделение ответственностей (SRP)
   - Использование слоев абстракции
   - Явные параметры вместо глобального состояния

5. **Promise.race для элегантной обработки таймаутов:**
   ```javascript
   const result = await Promise.race([
     actualWork(),
     timeout(1000)
   ]);
   ```

6. **Контроллер - точка сборки:**
   - Контроллер имеет право на высокое знание о зависимостях
   - В контроллере происходит интеграция компонентов
   - Высокая связность в контроллере - это нормально
   - Контроллер знает о протоколе (HTTP), модули - нет

7. **Зацепление на нижестоящие слои - допустимо:**
   - timeout может зависеть от delay
   - Контроллер может зависеть от сервисов
   - Сервисы могут зависеть от утилит
   - Но НЕ наоборот!

8. **Middleware - источник зацепления:**
   - Middleware часто создают скрытые зависимости
   - Порядок middleware становится критичным
   - Лучше использовать явные функции и контроллер

### Практический чеклист

**При написании нового модуля:**
- ✅ Все зависимости передаются явно (параметры/конструктор)
- ✅ Модуль не использует глобальное состояние
- ✅ Модуль не знает о деталях других модулей
- ✅ Модуль можно протестировать изолированно
- ✅ Модуль имеет одну четкую ответственность

**При рефакторинге:**
- ✅ Выявите все зацепления (используйте цветовое кодирование)
- ✅ Извлеките зависимости в параметры
- ✅ Создайте тонкие интерфейсы
- ✅ Используйте слои абстракции
- ✅ Соберите все в контроллере

**Помните:**
> "Зацепление между модулями должно быть минимальным и осуществляться только через четкие интерфейсы. Связность внутри модуля должна быть максимальной - все элементы работают вместе для достижения единой цели."

---

## Дополнительные материалы

### Связанные паттерны GRASP

- **Pure Fabrication (Чистая выдумка)** - создание классов для снижения зацепления
- **Indirection (Посредник)** - добавление промежуточного слоя
- **Protected Variations (Защита от изменений)** - использование интерфейсов

### Рекомендуемые лекции

1. "Pure Fabrication в JavaScript" - продолжение темы снижения зацепления
2. "API на Node.js" - как правильно парсить параметры и строить роутинг
3. "HTTP сервер на Node.js" - глубокое понимание протокола

### Репозиторий с примерами

Все примеры кода из этой лекции доступны в репозитории:
- **Плохой пример**: `/examples/coupling-bad` (Express с middleware)
- **Хороший пример**: `/examples/coupling-good` (чистые функции и контроллер)

### Для самостоятельного изучения

**Задание 1**: Рефакторинг класса Person
- Возьмите пример с классом Person из начала лекции
- Примените принцип Pure Fabrication
- Создайте отдельный PersonRepository для работы с БД

**Задание 2**: Анализ собственного кода
- Выберите один из ваших модулей
- Выделите цветом внутренние связи (синий) и зацепления (красный)
- Определите возможности для рефакторинга

**Задание 3**: Создание независимых утилит
- Напишите 3-5 независимых утилит по аналогии с delay
- Убедитесь, что они не зависят от внешних модулей
- Покройте их тестами без моков
