# Паттерн Facade (Фасад)

## Обзор

**Фасад** — это структурный паттерн проектирования, который предоставляет простой интерфейс к сложной подсистеме, содержащей множество взаимосвязанных классов. Фасад изолирует внутреннюю сложность подсистемы, предоставляя наружу простой и понятный API.

**Основная идея**: Скрыть сложность внутренней реализации за простым, стабильным интерфейсом.

---

## Отличие от похожих паттернов

### Фасад vs Адаптер

**Адаптер**:
- Закрывает **одну абстракцию** (один класс или объект)
- Преобразует интерфейс одного класса в другой

**Фасад**:
- Закрывает и изолирует **целую подсистему** (много классов)
- Упрощает работу со множеством взаимосвязанных компонентов

### Фасад vs Прокси

**Прокси**:
- Контролирует доступ к одному объекту

**Фасад**:
- Похож на прокси, но для **больших подсистем**
- Создаёт архитектурную границу между модулями

---

## Зачем нужен паттерн Фасад?

### 1. Создание архитектурной границы

Фасад **не позволяет "рыться в кишках" подсистемы**. Он создаёт чёткую архитектурную границу, которую нельзя пересекать.

```javascript
// ❌ Без фасада - доступ ко всем внутренним компонентам
const logger = new EventLogger();
const task = new Task('myTask', 5000, () => {});
task.on('done', () => logger.log('Task completed'));
task.run();

// ✅ С фасадом - простой интерфейс
const scheduler = new Scheduler();
scheduler.task('myTask', 5000, () => {
  console.log('Task executed');
});
```

### 2. Изоляция сложности

Подсистемой становится **проще пользоваться**. Вся внутренняя сложность скрыта за простым интерфейсом.

**Пример**: Внутри планировщика (scheduler) может быть:
- Сложная система таймеров
- Конфигурация
- Логирование событий
- Выполнение на отдельных процессах/потоках

**Снаружи**: Всего одна функция `scheduler.task()` для добавления задачи.

### 3. Контрактное программирование

Одна подсистема взаимодействует с другой **через определённый контракт**, от которого нельзя отступать.

**Архитектурная граница должна быть**:
- Покрыта тестами
- Покрыта типизацией (TypeScript)
- Воспринимается программистами как **рубеж, который нельзя пересекать**

### 4. Стабильность интерфейса

Создаётся **простой и стабильный интерфейс**. Внутри подсистемы можно свободно менять реализацию, **не меняя внешнего интерфейса**.

**Возможные изменения внутри**:
- Рефакторинг с использованием паттерна Strategy
- Переход на многопоточность (worker threads)
- Изменение механизма логирования
- Замена хранилища данных

**Клиентский код при этом не изменяется!**

---

## Недостатки паттерна Фасад

### 1. Потери производительности (Performance Penalty)

Дополнительный уровень абстракции **забирает ресурсы**:
- Дополнительные вызовы функций
- Обёртывание данных
- Косвенность (indirection)

```javascript
// Прямой вызов (быстрее)
task.execute();

// Через фасад (медленнее, но проще и безопаснее)
scheduler.task('myTask', 1000, () => task.execute());
```

### 2. Технический долг: антипаттерн God Object

Фасады имеют **тенденцию разрастаться** в антипаттерн **God Object** (Божественный объект).

**Как это происходит**:

```javascript
// Начало - простой фасад
class Scheduler {
  task(name, time, fn) { /* ... */ }
}

// Со временем добавляются новые методы
class Scheduler {
  task(name, time, fn) { /* ... */ }
  repeatingTask(name, interval, fn) { /* ... */ }
  stopTask(name) { /* ... */ }
  pauseTask(name) { /* ... */ }
  resumeTask(name) { /* ... */ }
  getTaskStatus(name) { /* ... */ }
  getAllTasks() { /* ... */ }
  clearAllTasks() { /* ... */ }
  // ... и так далее
}
```

**Проблема**: Когда подсистемы имеют **большое зацепление** (coupling), фасад разрастается.

**Решение**: Периодически извлекать абстракции из подсистемы, чтобы уменьшить объём фасада.

---

## Практический пример: Scheduler (Планировщик задач)

Рассмотрим реализацию простого планировщика задач, похожего на cron, но работающего in-memory.

### Структура подсистемы

```
Scheduler (Фасад)
├── EventLogger (логирование событий)
├── Task (базовый класс задачи)
│   ├── Наследует от EventEmitter
│   ├── Может выполняться по timeout
│   └── Может выполняться по interval
└── TaskRunner (управление задачами)
```

### Компонент 1: EventLogger

```javascript
// EventLogger - логирование событий планировщика
class EventLogger {
  log(message) {
    console.log(`[${new Date().toISOString()}] ${message}`);
  }

  error(message, err) {
    console.error(`[${new Date().toISOString()}] ERROR: ${message}`, err);
  }

  info(message) {
    console.info(`[${new Date().toISOString()}] INFO: ${message}`);
  }
}
```

**Важно**: Logger может быть заменён — может писать в базу данных, в файл, отправлять в систему мониторинга и т.д.

### Компонент 2: Task (Задача)

```javascript
const { EventEmitter } = require('events');

// Task - базовый класс для задач планировщика
class Task extends EventEmitter {
  constructor(name, time, fn, isInterval = false) {
    super();
    this.name = name;
    this.time = time;        // Задержка в миллисекундах
    this.fn = fn;            // Функция для выполнения
    this.isInterval = isInterval;  // timeout или interval
    this.timerId = null;
  }

  run() {
    this.emit('start', this.name);

    if (this.isInterval) {
      // Повторяющаяся задача через setInterval
      this.timerId = setInterval(() => {
        try {
          this.fn();
          this.emit('tick', this.name);
        } catch (err) {
          this.emit('error', this.name, err);
        }
      }, this.time);
    } else {
      // Одноразовая задача через setTimeout
      this.timerId = setTimeout(() => {
        try {
          this.fn();
          this.emit('done', this.name);
        } catch (err) {
          this.emit('error', this.name, err);
        }
      }, this.time);
    }
  }

  stop() {
    if (this.timerId) {
      if (this.isInterval) {
        clearInterval(this.timerId);
      } else {
        clearTimeout(this.timerId);
      }
      this.timerId = null;
      this.emit('stop', this.name);
    }
  }
}
```

**Примечание**: В данной реализации используется флаг `isInterval` для различения типов задач. **Лучшим решением** было бы использовать паттерн **Strategy** с двумя наследниками:
- `IntervalTask` — для повторяющихся задач
- `TimeoutTask` — для одноразовых задач

Однако текущая реализация показывает, что мы можем **рефакторить внутреннюю структуру, не меняя внешнего интерфейса фасада**.

### Компонент 3: Scheduler (Фасад)

```javascript
// Scheduler - фасад для управления задачами
class Scheduler extends EventEmitter {
  constructor() {
    super();
    this.tasks = new Map();  // Коллекция задач
    this.logger = new EventLogger();
  }

  // Главный метод фасада - добавление задачи
  task(name, time, fn) {
    // Создание экземпляра задачи
    const taskInstance = new Task(name, time, fn);

    // Добавление в коллекцию
    this.tasks.set(name, taskInstance);

    // Подписка на события задачи
    taskInstance.on('start', (taskName) => {
      this.logger.log(`Task "${taskName}" started`);
    });

    taskInstance.on('done', (taskName) => {
      this.logger.log(`Task "${taskName}" completed`);
      this.emit('taskDone', taskName);
    });

    taskInstance.on('error', (taskName, err) => {
      this.logger.error(`Task "${taskName}" failed`, err);
      this.emit('taskError', taskName, err);
    });

    // Запуск задачи
    taskInstance.run();

    return this; // Для цепочки вызовов
  }

  // Остановка конкретной задачи
  stop(name) {
    const task = this.tasks.get(name);
    if (task) {
      task.stop();
      this.tasks.delete(name);
    }
  }

  // Остановка всех задач
  stopAll() {
    for (const [name, task] of this.tasks) {
      task.stop();
    }
    this.tasks.clear();
  }
}
```

### Использование фасада (Usage)

```javascript
// ============================================
// USAGE SECTION (находится в другом файле)
// ============================================

const scheduler = new Scheduler();

// Подписка на события планировщика (опционально)
scheduler.on('taskDone', (taskName) => {
  console.log(`✅ Задача "${taskName}" выполнена!`);
});

scheduler.on('taskError', (taskName, err) => {
  console.error(`❌ Ошибка в задаче "${taskName}":`, err.message);
});

// Добавление задач через простой интерфейс
scheduler.task('sendEmail', 3000, () => {
  console.log('📧 Отправка email...');
  // Логика отправки email
});

scheduler.task('cleanupTemp', 5000, () => {
  console.log('🧹 Очистка временных файлов...');
  // Логика очистки
});

scheduler.task('backupDatabase', 10000, () => {
  console.log('💾 Резервное копирование базы данных...');
  // Логика бэкапа
});

// Остановка конкретной задачи
setTimeout(() => {
  scheduler.stop('sendEmail');
}, 8000);

// Остановка всех задач при завершении программы
process.on('SIGINT', () => {
  console.log('\n🛑 Остановка всех задач...');
  scheduler.stopAll();
  process.exit(0);
});
```

---

## Ключевые наблюдения из примера

### 1. Изоляция сложности

**Клиентский код не знает**:
- Что внутри есть класс `Task`
- Как реализованы таймеры
- Как работает система событий
- Что используется `EventEmitter`

**Клиентский код видит только**:
```javascript
scheduler.task(name, time, fn);
```

### 2. Свобода рефакторинга

Мы можем изменить внутреннюю реализацию:

#### Рефакторинг 1: Использование Strategy

```javascript
// Базовый класс задачи
class Task extends EventEmitter {
  constructor(name, strategy, fn) {
    super();
    this.name = name;
    this.strategy = strategy; // Стратегия выполнения
    this.fn = fn;
  }

  run() {
    this.strategy.execute(this);
  }
}

// Стратегия для timeout
class TimeoutStrategy {
  constructor(time) {
    this.time = time;
  }

  execute(task) {
    setTimeout(() => {
      task.fn();
      task.emit('done', task.name);
    }, this.time);
  }
}

// Стратегия для interval
class IntervalStrategy {
  constructor(interval) {
    this.interval = interval;
  }

  execute(task) {
    setInterval(() => {
      task.fn();
      task.emit('tick', task.name);
    }, this.interval);
  }
}
```

**Важно**: Интерфейс фасада `scheduler.task(name, time, fn)` **остаётся неизменным**!

#### Рефакторинг 2: Использование Worker Threads

```javascript
const { Worker } = require('worker_threads');

class Scheduler extends EventEmitter {
  task(name, time, fn) {
    // Теперь задачи выполняются в отдельных потоках
    const worker = new Worker('./task-worker.js', {
      workerData: { name, time, fn: fn.toString() }
    });

    worker.on('message', (msg) => {
      if (msg.type === 'done') {
        this.emit('taskDone', name);
      }
    });

    // Внешний интерфейс НЕ ИЗМЕНИЛСЯ!
  }
}
```

### 3. Архитектурная граница

```
┌─────────────────────────────────────────┐
│         Клиентский код                  │
│  (бизнес-логика приложения)             │
└──────────────┬──────────────────────────┘
               │
               │ scheduler.task(...)
               │
┌──────────────▼──────────────────────────┐
│         ФАСАД (Scheduler)               │ ← Архитектурная граница
│  - Простой интерфейс                    │
│  - Стабильный контракт                  │
│  - Покрыт тестами и типами              │
└──────────────┬──────────────────────────┘
               │
    ┌──────────┴──────────┐
    │                     │
┌───▼────┐          ┌────▼─────┐
│  Task  │          │  Logger  │
└────────┘          └──────────┘
    │                     │
  (Сложная внутренняя структура)
```

**Преимущества границы**:
- Бизнес-код не зависит от внутренних изменений
- Можно безопасно рефакторить подсистему
- Упрощается тестирование
- Улучшается читаемость кода

---

## Принцип проектирования интерфейса фасада

### Интерфейс принадлежит клиенту

> **Важный принцип**: Интерфейс фасада — это **атрибут клиентской стороны**, а не реализующей стороны.

**Клиентская сторона** — та, которая **использует** абстракцию.
**Серверная сторона** — та, которая **предоставляет** услуги.

```javascript
// ❌ Неправильно - проектировать интерфейс от реализации
class Scheduler {
  addTaskToCollection(task) { /* ... */ }
  createTimerForTask(task) { /* ... */ }
  attachEventListeners(task) { /* ... */ }
  // Эти методы отражают ВНУТРЕННЮЮ реализацию
}

// ✅ Правильно - проектировать интерфейс от потребностей клиента
class Scheduler {
  task(name, time, fn) { /* ... */ }
  // Этот метод отражает то, что НУЖНО клиенту
}
```

### Клиент-серверное взаимодействие (в контексте архитектуры)

**Это не про сетевое взаимодействие!**

Это про разделение ответственности:
- **Клиент** — использует сервис
- **Сервер** — предоставляет сервис

**Архитектурная граница** между ними:
1. Сохраняет сложность внутри
2. Не позволяет сложности "просочиться" в клиентский код
3. Фасад может спокойно использоваться в бизнес-логике

```javascript
// Бизнес-логика (клиент)
class OrderService {
  async processOrder(order) {
    // Используем планировщик через простой интерфейс
    scheduler.task('sendConfirmation', 1000, () => {
      this.sendEmail(order.customer.email);
    });

    // Не нужно знать про Task, EventLogger и т.д.
  }
}
```

---

## Когда использовать паттерн Фасад

### ✅ Используйте Фасад, когда:

1. **Нужно упростить работу со сложной подсистемой**
   ```javascript
   // Вместо работы с множеством классов
   const logger = new Logger();
   const validator = new Validator();
   const parser = new Parser();
   const transformer = new Transformer();

   // Один фасад
   const dataProcessor = new DataProcessor();
   dataProcessor.process(data);
   ```

2. **Нужно создать архитектурную границу между слоями**
   ```
   Presentation Layer → Facade → Business Logic Layer
   ```

3. **Нужна стабильность при изменении реализации**
   - Рефакторинг внутренних компонентов
   - Оптимизация производительности
   - Добавление новых возможностей

4. **Хотите уменьшить зависимости между модулями**
   - Низкое зацепление (low coupling)
   - Высокая связность (high cohesion)

### ❌ Не используйте Фасад, когда:

1. **Подсистема уже проста**
   - Добавление фасада только усложнит код

2. **Нужен доступ к внутренним компонентам**
   - Фасад скроет детали, которые могут понадобиться

3. **Производительность критична**
   - Каждый уровень абстракции имеет стоимость

---

## Связь с другими паттернами

### Фасад + Singleton

```javascript
// Часто фасад реализуется как одиночка
class Scheduler {
  static #instance = null;

  static getInstance() {
    if (!Scheduler.#instance) {
      Scheduler.#instance = new Scheduler();
    }
    return Scheduler.#instance;
  }

  // ... методы фасада
}

// Использование
const scheduler = Scheduler.getInstance();
```

### Фасад + Factory

```javascript
class Scheduler {
  task(name, time, fn) {
    // Фасад использует фабрику для создания задач
    const task = TaskFactory.create(time, fn);
    // ...
  }
}
```

### Фасад + Strategy

```javascript
class Scheduler {
  constructor(executionStrategy) {
    this.strategy = executionStrategy; // Можно менять стратегию
  }

  task(name, time, fn) {
    this.strategy.execute(name, time, fn);
  }
}
```

---

## Ключевые выводы

### 1. Основная цель фасада
**Изоляция сложности** подсистемы за **простым и стабильным интерфейсом**.

### 2. Преимущества
- ✅ Упрощение использования сложных подсистем
- ✅ Создание архитектурных границ
- ✅ Свобода рефакторинга внутренней реализации
- ✅ Уменьшение зависимостей между модулями
- ✅ Контрактное программирование

### 3. Недостатки
- ❌ Снижение производительности (performance penalty)
- ❌ Риск превращения в God Object
- ❌ Дополнительный уровень абстракции

### 4. Золотое правило
> Интерфейс фасада должен отражать **потребности клиента**, а не **детали реализации**.

### 5. Архитектурная граница
Фасад — это **рубеж, который нельзя пересекать**:
- Должен быть покрыт тестами
- Должен быть типизирован
- Должен быть стабильным

### 6. Когда рефакторить
Если фасад разрастается — **извлекайте абстракции** из подсистемы наружу, чтобы уменьшить объём интерфейса.

---

## Дополнительные примеры использования

### Пример 1: Фасад для работы с файловой системой

```javascript
class FileSystem {
  async saveFile(path, content) {
    // Внутри: валидация, создание директорий, запись, логирование
    await this.#validatePath(path);
    await this.#ensureDirectory(path);
    await this.#writeFile(path, content);
    await this.#logOperation('save', path);
  }

  async readFile(path) {
    // Внутри: проверка существования, чтение, кеширование
    await this.#checkExists(path);
    const content = await this.#readFromDisk(path);
    this.#cache.set(path, content);
    return content;
  }

  // Приватные методы скрыты
  #validatePath(path) { /* ... */ }
  #ensureDirectory(path) { /* ... */ }
  #writeFile(path, content) { /* ... */ }
  #logOperation(type, path) { /* ... */ }
}

// Использование
const fs = new FileSystem();
await fs.saveFile('/data/user.json', JSON.stringify(user));
```

### Пример 2: Фасад для HTTP-клиента

```javascript
class ApiClient {
  async get(endpoint) {
    // Внутри: добавление заголовков, обработка ошибок, retry логика
    return this.#request('GET', endpoint);
  }

  async post(endpoint, data) {
    return this.#request('POST', endpoint, data);
  }

  #request(method, endpoint, data) {
    // Сложная логика: headers, auth, retries, error handling
  }
}

// Использование
const api = new ApiClient();
const users = await api.get('/users');
```

---

## Практические рекомендации

### 1. Держите фасад простым
```javascript
// ✅ Хорошо
class Scheduler {
  task(name, time, fn) { /* ... */ }
  stop(name) { /* ... */ }
}

// ❌ Плохо - слишком много методов
class Scheduler {
  task() { /* ... */ }
  repeatingTask() { /* ... */ }
  dailyTask() { /* ... */ }
  weeklyTask() { /* ... */ }
  monthlyTask() { /* ... */ }
  cronTask() { /* ... */ }
  // ... ещё 20 методов
}
```

### 2. Используйте TypeScript для контракта

```typescript
interface IScheduler {
  task(name: string, time: number, fn: () => void): void;
  stop(name: string): void;
}

class Scheduler implements IScheduler {
  // Реализация гарантированно соответствует контракту
}
```

### 3. Покрывайте тестами

```javascript
describe('Scheduler Facade', () => {
  it('should execute task after specified time', (done) => {
    const scheduler = new Scheduler();
    const startTime = Date.now();

    scheduler.task('test', 100, () => {
      const elapsed = Date.now() - startTime;
      expect(elapsed).toBeGreaterThanOrEqual(100);
      done();
    });
  });

  it('should stop task by name', () => {
    const scheduler = new Scheduler();
    scheduler.task('test', 1000, () => {});
    scheduler.stop('test');

    expect(scheduler.tasks.has('test')).toBe(false);
  });
});
```

### 4. Документируйте контракт

```javascript
/**
 * Планировщик задач - фасад для управления отложенным выполнением функций
 *
 * @example
 * const scheduler = new Scheduler();
 * scheduler.task('cleanup', 5000, () => {
 *   console.log('Cleaning up...');
 * });
 */
class Scheduler {
  /**
   * Добавляет задачу в планировщик
   * @param {string} name - Уникальное имя задачи
   * @param {number} time - Задержка в миллисекундах
   * @param {Function} fn - Функция для выполнения
   */
  task(name, time, fn) { /* ... */ }
}
```

---

## Заключение

Паттерн **Фасад** — это мощный инструмент для создания **простых интерфейсов** к **сложным подсистемам**. Он позволяет:

1. **Изолировать** внутреннюю сложность
2. **Создавать** архитектурные границы между модулями
3. **Обеспечивать** стабильность внешнего API
4. **Упрощать** использование подсистемы

При правильном применении фасад делает код более **понятным**, **поддерживаемым** и **гибким** к изменениям.

**Главное правило**: Проектируйте интерфейс фасада исходя из **потребностей клиента**, а не из **деталей реализации**.

---

## Дополнительные материалы

### Связанные паттерны
- **Adapter** — преобразование интерфейса одного класса
- **Proxy** — контроль доступа к объекту
- **Mediator** — координация взаимодействия между объектами
- **Strategy** — инкапсуляция алгоритмов

### Принципы проектирования
- **Low Coupling** — низкое зацепление между модулями
- **High Cohesion** — высокая связность внутри модуля
- **Interface Segregation Principle** — клиенты не должны зависеть от методов, которые не используют
- **Dependency Inversion Principle** — зависимость от абстракций, а не от конкретных реализаций

### Рекомендуемая литература
- "Design Patterns: Elements of Reusable Object-Oriented Software" (GoF)
- "Clean Architecture" by Robert C. Martin
- "Patterns of Enterprise Application Architecture" by Martin Fowler
